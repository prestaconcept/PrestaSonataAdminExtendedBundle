
sonata_block:
    blocks:
        presta_sonata_admin_extended.block.dashboard.user:

sonata_admin:
    dashboard:
        blocks:
            - { position: left, type: presta_sonata_admin_extended.block.dashboard.user }
